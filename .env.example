# Configurazione locale - NON committare questo file (vedi .gitignore)
# In produzione si possono usare direttamente le variabili d'ambiente del
# container: hanno la precedenza su questo file.

APP_ENV=development

DB_HOST=mariadb_local
DB_NAME=s6313243
DB_USER=root
DB_PASS=cambiami

# Chiave di firma dei JWT (generata con: php -r 'echo bin2hex(random_bytes(32));')
JWT_SECRET=
JWT_TTL=3600
